syntax = "proto3";

option go_package = "/pb";

message Jugador {
    string id = 1;
    bool bot = 2;
    bool vive = 3;
}

service JugadorLiderService {
    rpc SolicitarUnirce(Jugador) returns (Jugador) {};
}